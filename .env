# ===== ClaraVerse - Example .env (NÃO COLOQUE SEGREDOS REAIS AQUI) =====

# Flask
# Em produção, defina SECRET_KEY no Render (não comite o valor real).
SECRET_KEY=troque-esta-chave-super-secreta

# Banco de dados (SQLite por padrão)
DATABASE_URL=sqlite:///data.db

# OpenAI (NÃO comitar chave real; defina no Render)
OPENAI_API_KEY=coloque_no_Render

# Binance
# true => usa testnet/simula; false => produção (cuidado!)
BINANCE_TESTNET=true
# Normalmente deixe em branco; se quiser forçar testnet API base, pode usar:
# BINANCE_API_BASE=https://testnet.binance.vision
BINANCE_API_BASE=
# PAPER_TRADING=true => não envia ordens na Binance (simula e loga no SQLite)
PAPER_TRADING=true

# Criptografia (Fernet) — gere uma chave e defina no Render:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=coloque_no_Render